inp = """

## Языки и диалекты
1- ==C== — низкоуровневый
1- ==C++== — много парадигм
2- ==Java== — чистое ООП, надёжность
1- ==python== — простой, многопрофильный
1- ==Go== — быстрый, удобный
2- ==JavaScript== — язык программирования, используется для написания сценариев
2- ==TypeScript== — язык программирования, развитый JavaScript
1* ==SQL== — язык запросов к БД
2* ==UML== — это стандартизированный графический язык, используемый для визуализации, описания, проектирования и документирования систем
2* ==ASM== — assembly/assembler, язык и компилятор кода непосредственно команд для процессора
2- ==Bash== — ...
* 

## Фреймворки
3- ==React== (JS) — библиотека создания пользовательских интерфейсов
3- ==Flask== (python) — создание веб-приложений, легковесный, синхронный
2- ==viper== (Go) — библиотека, предназначенная для управления конфигурацией приложений
2- ==cobra== (Go) — библиотека для создания командных интерфейсов (CLI): команды, подкоманды, флаги и опции
3* ==Django== (python) — free высокоуровневый веб-фреймворк, для быстрого и безопасного создания и поддержания сайтов
* 

## Библиотеки и команды
2* ==marshmalow== (python) — это библиотека, используемая для валидации и сериализации/десериализации данных
3* ==DPDK== — Data Plane Development Kit — набор open-source библиотек и драйверов для высокопроизводительной обработки сетевых пакетов (kernel bypass)
3* ==Solarflare OpenOnload== — open-source либа для kernel bypass работы с сетевой картой. Даёт макс. производительность с сетевыми адаптерами Solarflare
2* ==epoll==, ==select==, ==kqueue== — особые syscall для работы с переферией
* 

## Системы
2- ==docker== — система упаковки приложения и всех зависимостей в контейнер, который можно запустить на любом устройстве, где установлен docker
2- ==Kubernetes== — Инструмент оркестрации контейнеров (управление, мониторинг, деплой, маштабирование). Обычно работает с кластером Master node + `[]`worker_nodes
1- ==git== — распределённая система управления версиями
2* ==github== — репозиторий
2- ==Grafana== — система визуализации данных
2- ==Grafana Loki== — система хранения и управления логами
2- ==Prometheus== — система мониторинга и оповещения, предназначена для сбора и анализа метрик приложений и инфраструктуры (с высокой частотой, временные ряды)
3- ==Beats== — легковесные агенты отправляющие данные в Elasticsearch
3- ==Elasticsearch== — распределенная поисковая и аналитическая система, основанная на Lucene. Документоориентирована
1- ==Kafka== — распределенная система обмена сообщениями между серверными приложениями в режиме реального времени. (Распределённый реплицируемый лог)
3- ==DockerHub== — облачный реестр контейнеров, предоставляемый компанией Docker
3- ==Nginx== — веб-сервер и почтовый прокси сервер. Способен обрабатывать огромное количество запросов
3- ==Apache== — веб-сервер. Надёжный и с гибкими настройками
2- ==Swagger== — инструмент для генерации OAS по коду, или шаблона кода по OAS
2* ==ClickHouse== — high-perf open-source столбцовая СУБД
2* ==Hadoop== — open-source прогр. платформа для распределенного хранения (кластеры) и обработки (MR) данных
2* ==Spark== — система быстрой (in-memory) обработки данных как локально так и на кластерах, может работать поверх HDFS (от hadoop) через YARN
3* ==Airflow== —  open-source платформа для шедулинга и мониторинга тасок (например с помощью Spark)
2* ==Bruno== — альтернатива Postman, удобный open-source инструмент для тестирования HTTP-API (а также GraphQL-запросов)
3* ==NodeJS== — это среда выполнения JavaScript, которая позволяет запускать код на языке JavaScript вне браузера, в основном для создания серверных (backend) веб-приложений
3* ==Webpack== — open-source сборщик модулей, собирает JS-модули и пр (CSS, JPG, etc) в один или несколько файлов (бандлов), готовых для использования в браузере
3* ==LLVM== — набор технологий и инфраструктура для создания компиляторов и инструментальных средств. Он состоит из трех основных частей: фронтенда, который обрабатывает исходный код на языках высокого уровня, оптимизатора (миддленда), который улучшает код, и бэкенда, который генерирует машинный код для целевой архитектуры. LLVM использует низкоуровневое промежуточное представление (LLVM IR) для выполнения оптимизаций, что упрощает поддержку новых языков и архитектур
3* ==GCC== — GNU Compiler Collection, один из популярных компиляторов для C, C++ и др.
2* ==Clang== — ещё один популярный компилятор. Построен поверх LLVM
2* ==GDB==, ==LLDB== (где DB - debugger) — отладчики, работающие с компиляторами GCC и Clang соответственно
2* ==make== — утилита, автоматизирующая сборку для маленьких и средних проектов на одной платформе по простому принципу:
```bash
target: dependencies
	command
```
2* ==cmake== — кросс-платформенный генератор сборщиков, генерирует `MakeFile` и другие (под разные сборщики) из `CMakeList.txt` файлов. Используется для больших проектов
3* ==ASan==, ==UBSan==, ==TSan== — санитайзеры, инструменты обнаружения ошибок с адресами, UB, потоками соответственно. Доступны и для GCC и для Clang. Обычно используются в самих компиляторах при включении определенных флагов, т.к. являются рантайм инструментами (как и Valgrind)
3* ==Valgrind== — инструментальное программное обеспечение, предназначенное для отладки использования памяти, обнаружения утечек памяти, а также профилирования
2* ==perf== — имеет разные смыслы: perf events это встроенные счётчики ядра Linux; `perf` встроенный профилировщик Linux, который собирает профиль; VTune (то же, что `perf` однако проприетарный от Intel и имеет свой GUI)
3* ==Bazel== — open-source инструмент от google для автоматизации сборки и тестирования
* 

## Спецификации
2- ==CGI== (common gateway interface) — стандарт интерфейса связи веб-сервера с программой
2- ==WSGI== (web server gateway interface) — протокол взаимодействия веб-сервра с python-программой
2- ==ASGI== (asynchronous server gateway interface) — асинхронный WSGI
3- ==DOM== — объектная модель документа, интерфейс представления узлов размеченного файла как объектов
2- ==HTTP== — стандартный протокол сети (GET, POST и т.д.)
2- ==ORM== — технология работы с БД. Прослойка превращающая данные БД в объекты программы и наоборот
2- ==TLS== (==SSL==) — протокол безопасности, обычно между клиентом и сервером
2- ==SSH== — также протокол безопасности, но для удалённого управления сервером и scp команд
3* ==FIX==-protocol — Financial Information eXchange, международный стандарт для электронного обмена сообщениями между участниками финансового рынка в реальном времени
3- ==HTML== — язык разметки, в основном для контента и с предопределёнными тегами
3- ==XML== — язык разметки, в основном для данных. Теги кастомные
1- ==JSON== — формат обмена данными. Самый распространённый
2- ==YAML== — язык сериализации данных, простой. Часто используется для конфигурационных файлов
3- ==СSS== — язык стилей (часто над html доком)
1- ==protobuf== — механизм сериализации структурированных данных
2- ==REST== — подход. HTTP/1.1, JSON/XML, запрос/ответ. Медленный, нестрогий, для веба
2- ==gRPC== — подход. HTTP/2, protobuf, динамика. Быстрый, строгий, для сервисов
2- ==TCP== — основной протокол передачи данных в инете. Пакетный. Надежный, но медленее UDP
3- ==IPC== — Inter-Process Communication, механизм взаимодействия между процессами. Часто об UNIX-сокетах
2- ==OSI== (Open Systems Interconnection) — это концептуальная модель сетевого взаимодействия, описанная в виде семиуровневой архитектуры (физический, сетевой, транспортный, прикладной и другие)
2- ==OAuth2.0==, ==JWT==, ==Cookie== — протокол авторизации, формат токена и метод хранения сессий соответственно
3- ==CWE==, ==CVE== — перечень уязвимостей ПО. Первый больше о категориях, а второй как конкретный id конкретной проблемы
2- ==OAS== — OpenAPI Specification
3- ==RFC== — Request For Comments, рабочее предложение обсуждаемое и принимаемое для реализации. В общепринятом смысле это про спецификации мирового интернета
1- ==SDK== — полный набор для разработки ПО: либы+апи, дока + примеры, инструменты(компиляторы, дебаггеры, отладчики)
2* ==C4== — простой метод графической записи для моделирования архитектуры программных систем
2* ==ERD== (Entity-Relationship Diagram) — диаграмма сущность — связь, часто описывают реляционные БД
2* ==Flamegraph== — один из способов отображения информации профиля, собранного профилировщиком
3* ==Unicast==, ==Broadcast==, ==Multicast== — разные схемы вещания сетевого трафика. Соответственно: 1-к-1, 1-всем кто поймает, 1-подписчикам multicast IP
3* ==SIMD== — Single Instruction, Multiple Data — принцип обеспечение параллелизма на уровне данных
3* ==SSE==, ==AVX== — Streaming SIMD Extension & Advanced Vector Extensions — наборы расширенных инструкций процессор для векторизации. AVX явл. приемником SSE
3* ==lock-free== структуры — работающие без mutex, исключает deadlock (но какой то поток может вечно "проигрывать"). Использует атомарные примитивы, аппаратную гарантию эксклюзии cache-line
3* ==wait-free== структуры — используют алгоритмы с математической гарантией того, что каждый поток завершится за конечное число шагов. Является более строгим чем lock-free
* 

## Принципы
1 ==CRUD== — создание (create), чтение (read), модификация (update), удаление (delete)
1- ==KISS== — Keep It Simple Simon
2 ==YAGNI== — You Aren't Gonna Need It (Вам это не понадобится). О том что не надо пытаться написать сразу всё
1- ==DRY== — Don’t Repeat Yourself
1 ==BDUF== — Big Design Up Front (Сначала большое проектирование)
2- ==APO== — Избегайте преждевременной оптимизации
1- ==S.O.L.I.D== — SRP принцип единой ответственности, OCP принцип открытости/закрытости, LSP принцип замещения Лисков, ISP принцип разделения интерфейса, DIP принцип инверсии зависимостей
1- ==TTP== — Three-Tier Pattern, принцип разделения на уровни данных, логики и представления.
2- ==TDD== — Test-Driven Development
2- ==CQS== — Command-Query Separation, принцип разделения методов на команды (изменения) и запросы (получение)
1- ==PoLA== — Principle of Least Astonishment, принцип наименьшего удивления, создавать ожидаемое поведение
2- ==DDD== — Domain-Driven Design, создание модели, отражающей реальные бизнес-процессы, и взаимодействие с ней через понятный интерфейс
2- ==MVC== — Model-View-Controller, контроллер(C) управляет моделью (M) и её связью с представлением (V)
3* ==zero-copy== техника — особый приём когда данные с сетевой карты (NIC) пишутся/читаются напрямую в user-space
"""

import re

result = {}

for line in inp.splitlines():
    # игнорируем пустые строки
    if not line.strip():
        continue

    # определяем ключ: цифра в начале или 3 по умолчанию
    m = re.match(r"\s*([1-3])\b", line)
    key = int(m.group(1)) if m else 3

    # ищем все совпадения вида ==text==
    matches = re.findall(r"==\s*(.*?)\s*==", line)

    # добавляем все найденные в словарь
    if matches:
        result.setdefault(key, []).extend(matches)

for k, v in result.items():
    print (k)
    print ('\t', end='')
    for el in v:
        print(f"{el}, ", end='')
    print()

print(sum(map(lambda k:len(result[k]), result)))